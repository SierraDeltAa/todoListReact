{
  "homepage": "http://SieraDeltAa.github.io/todoListReact",
  "name": "todoListReact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "start": "npm run clean && parcel serve ./src/index.html --open",
    "clean": "rm -rf .cache dist",
    "build": "npm run clean && parcel build ./src/index.html --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
